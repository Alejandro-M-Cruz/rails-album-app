<%= link_to (defined?(path) && path || :back), class: 'd-block no-format-link ms-4 mt-4 text-secondary' do %>
  <i class="bi bi-chevron-left"></i> <span class="fs-5"><%= defined?(text) && text || 'Back' %></span>
<% end %>